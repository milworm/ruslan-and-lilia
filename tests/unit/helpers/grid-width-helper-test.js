import { gridWidthHelper } from 'gallery/helpers/grid-width-helper';
import { module, test } from 'qunit';

module('Unit | Helper | grid width helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gridWidthHelper([42]);
  assert.ok(result);
});
